'use strict';

angular.module('pace.dashboard')
    .controller('PinterestCtrl', ['$scope', '$state', '_',
        function ($scope, $state, _) {
            /* jshint indent: false */    
        }
    ]);