'use strict';

angular.module('pace.dashboard')
    .controller('ResourcesCtrl', ['$scope', '$state', '_',
        function ($scope, $state, _) {
            /* jshint indent: false */
        }
    ]);