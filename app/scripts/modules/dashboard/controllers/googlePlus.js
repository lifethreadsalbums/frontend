'use strict';

angular.module('pace.dashboard')
    .controller('GooglePlusCtrl', ['$scope', '$state', '_',
        function ($scope, $state, _) {
            /* jshint indent: false */
        }
    ]);